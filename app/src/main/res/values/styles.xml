<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.VoiceTaskManager" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_accent</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="materialButtonStyle">@style/AppButtonStyle</item>
        <item name="android:statusBarColor">@color/dark_bg</item>
    </style>

    <!-- Global App Button Style -->
    <style name="AppButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/purple_accent</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="shapeAppearanceSmallComponent">@style/AppButtonShape</item>
    </style>

    <!-- Button Shape -->
    <style name="AppButtonShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Neon OTP Box -->
    <style name="NeonOtpBox">
        <item name="android:layout_width">44dp</item>
        <item name="android:layout_height">54dp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/neon_box_selector</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:inputType">number</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:padding">0dp</item>
    </style>

</resources>
